<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>

        {{#if edit}}
            <script src="/bower_components/angular/angular.min.js"></script>
            <script>
                angular.module('editApp', []);
                angular.element(document).ready(function() {
                    angular.bootstrap(document, ['editApp']);
                });
            </script>
        {{/if}}
    </head>
    <body>
        <p>I am a template A</p>

        {{#A}}
            {{> A}}
        {{/A}}

        {{#B}}
            {{> B}}
        {{/B}}

        {{#C}}
            {{> C}}
        {{/C}}
    </body>
</html>